// const text = "TRADITION • NUTRITION • TRANSFORMATION • ";
// const circularTextEl = document.getElementById("circularText");

// function renderCircularText() {
//   circularTextEl.innerHTML = "";
//   const container = circularTextEl.getBoundingClientRect();
//   const radius = container.width / 2.2;
//   const textLength = text.length;

//   for (let i = 0; i < textLength; i++) {
//     const char = text[i];
//     const span = document.createElement("span");
//     const angle = (360 / textLength) * i;
//     span.innerText = char;
//     span.style.transform = `rotate(${angle}deg) translate(${radius}px) rotate(90deg)`;
//     circularTextEl.appendChild(span);
//   }
// }

// window.addEventListener("load", renderCircularText);
// window.addEventListener("resize", renderCircularText);
